# Modifiez la fonction que vous avez écrite précédemment pour qu'elle  accepte 
# deux arguments supplémentaires min et max et qu’elle retourne un vecteur de n
# valeurs aléatoires comprises entre min et max avec la fonction runif(n, min, max).
# Tapez submit() dans la console une fois que vous avez écrit la fonction et sauvegardé le script.