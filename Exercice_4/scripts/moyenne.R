# Créer une fonction moyenne_arrondie, qui prend en argument un vecteur x et un argument facultatif decimales.
# La fonction doit effectuer les opérations suivantes :
#   
# - calculer la moyenne du vecteur
# - si decimales est défini, arrondir la moyenne au nombre de décimales correspondant avec la fonction round()
# - retourner le résultat
#
# Un argument non défini prend la valeur NULL
# 
# Tapez submit() dans la console après avoir enregistré le script une fois que vous avez fini
# La fonction is.null() permet de savoir si un objet a pour valeur NULL.

moyenne_arrondie <- function(x, decimales) {# Attention il faut modifier les arguments
  # Tapez votre code ici

}